{
  "name": "spotify-grid-card",
  "version": "0.1.0",
  "description": "Dark VisionOS-style Spotify grid Lovelace card (tap to play playlists).",
  "type": "module",
  "scripts": {
    "build": "node esbuild.mjs",
    "watch": "node esbuild.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "lit": "^3.1.0",
    "custom-card-helpers": "^1.13.0",
    "typescript": "^5.5.4"
  }
}